name: Deploy Docs

on:
  push:
    branches:
      - m
  workflow_call:

permissions:
  contents: write

jobs:
  deploy-docs:
    uses: George-Ogden/actions/.github/workflows/deploy-mkdocs.yaml@v4.6.4
